#include <stdio.h>

int main(void){
    int a = 10;

    if (-1 == a || a < 440){
        int chave = 3;
        printf("A chave é: %d\n", chave);
    }//Este bloco não precisa do if, chave fica neste contextom não pode ser usado fora

    printf("O resultado lógico é: %d", a ^ b);

	return 0;
}
